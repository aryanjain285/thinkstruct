# Copy to .env and fill in. .env is gitignored — never commit real keys.
OPENAI_API_KEY=sk-proj-...

# Optional overrides
# OPENSEARCH_HOST=http://localhost:9200
# PATSEARCH_EMBEDDER=openai-small
# HF_HUB_DISABLE_XET=1
